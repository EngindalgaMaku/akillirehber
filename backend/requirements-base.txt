# Base requirements - Large packages that rarely change
# These are installed first and cached separately

# Scientific computing and ML (large packages)
numpy>=1.26.0,<2
pandas>=2.2.0,<3
scikit-learn>=1.3.0
scipy>=1.11.0

# Original BERTScore dependencies (large packages)
torch>=2.2.0
transformers>=4.36.0
bert-score>=0.3.13

# Giskard RAG Evaluation Toolkit (RAGET) with dependencies
# Note: giskard[rag] includes datasets as a dependency, so we don't need to specify it separately
giskard[rag]>=2.0.0

# Required by Giskard RAGET embedding path (keeps install lighter than giskard[llm])
litellm>=1.0.0

# Required by Giskard RAGET plotting module import chain
bokeh>=3.0.0

# UMAP dependencies (required by Giskard)
umap-learn>=0.5.0,<0.6.0
numba>=0.60.0

# Weights & Biases (live run logging)
wandb>=0.16.0

# Note: torch is installed separately in Dockerfile for better caching control
