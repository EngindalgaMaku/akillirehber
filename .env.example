# ===========================================
# RAG Educational Chatbot - Environment Variables
# ===========================================
# Copy this file to .env and update the values

# ===========================================
# PostgreSQL Database Configuration
# ===========================================
POSTGRES_USER=raguser
POSTGRES_PASSWORD=ragpassword
POSTGRES_DB=ragchatbot

# ===========================================
# Backend Configuration
# ===========================================
# JWT Secret Key - CHANGE THIS IN PRODUCTION!
SECRET_KEY=your-super-secret-key-change-in-production-use-openssl-rand-hex-32

# JWT Algorithm
ALGORITHM=HS256

# Access Token Expiration (minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Environment (development, staging, production)
ENVIRONMENT=development

# ===========================================
# Service URLs (for internal Docker network)
# ===========================================
DATABASE_URL=postgresql://raguser:ragpassword@postgres:5432/ragchatbot
WEAVIATE_URL=http://weaviate:8080

# ===========================================
# Frontend Configuration
# ===========================================
NEXT_PUBLIC_API_URL=http://localhost:8000

# ===========================================
# Optional: HuggingFace Configuration
# ===========================================
# HuggingFace API Token for model downloads and higher rate limits
# Get your token from: https://huggingface.co/settings/tokens
# HF_TOKEN=hf_your-huggingface-token

# ===========================================
# Optional: OpenAI API Key (for agentic chunking)
# ===========================================
# OPENAI_API_KEY=sk-your-openai-api-key

# ===========================================
# Optional: LLM Provider API Keys (for RAGAS evaluation)
# ===========================================
# OpenRouter - Multi-model API with embeddings support
# OPENROUTER_API_KEY=sk-or-your-openrouter-key

# Claude.gg - Anthropic Claude models via compatible API
# CLAUDEGG_API_KEY=sk-542ca4c71cf0451a9b92b584eeba6520

# API Claude.gg - Multi-model provider (GPT-5, O3, Grok, DeepSeek, Gemini)
# APICLAUDEGG_API_KEY=sk-542ca4c71cf0451a9b92b584eeba6520

# Groq - Fast inference (free tier available)
# GROQ_API_KEY=gsk-your-groq-key

# z.ai - Anthropic-compatible API with GLM models
# ZAI_API_KEY=your-zai-api-key

# RAGAS Provider Selection (optional - auto-selects if not set)
# Options: openrouter, claudegg, apiclaudegg, groq, openai
# RAGAS_PROVIDER=apiclaudegg

# RAGAS Model Override (optional - uses provider default if not set)
# Examples: gpt-5, o3, deepseek-r1, claude-opus-4-5, openai/gpt-4o-mini
# RAGAS_MODEL=gpt-5

# ===========================================
# Optional: Alibaba Cloud DashScope API Key (for embeddings)
# ===========================================
# Alibaba DashScope - text-embedding-v4 model support
# Required only if using "alibaba/text-embedding-v4" as embedding model
# DASHSCOPE_API_KEY=sk-your-dashscope-api-key

# ===========================================
# Optional: Cohere API Key (for embeddings)
# ===========================================
# Cohere - Multilingual embedding models support
# Required only if using "cohere/embed-multilingual-*" models
# Get your API key from: https://dashboard.cohere.com/api-keys
# COHERE_API_KEY=your-cohere-api-key

# ===========================================
# Optional: Reranker Configuration
# ===========================================
# Reranker cache TTL in seconds (default: 300 = 5 minutes)
# RERANKER_CACHE_TTL=300

# Reranker timeout in seconds (default: 5)
# RERANKER_TIMEOUT=5

# Maximum documents per reranking request (default: 1000)
# RERANKER_MAX_DOCUMENTS=1000

# ===========================================
# Optional: Embedding Model Configuration
# ===========================================
# EMBEDDING_MODEL=all-MiniLM-L6-v2

# ===========================================
# Optional: SMTP Email Configuration
# ===========================================
# SMTP server hostname (e.g., smtp.gmail.com, smtp.office365.com)
# SMTP_HOST=smtp.gmail.com

# SMTP server port (default: 587 for TLS)
# SMTP_PORT=587

# SMTP username (usually your email address)
# SMTP_USERNAME=your-email@example.com

# SMTP password or app-specific password
# SMTP_PASSWORD=your-smtp-password

# From email address for outgoing emails
# SMTP_FROM_EMAIL=noreply@example.com

# Use TLS encryption (default: true)
# SMTP_USE_TLS=true
